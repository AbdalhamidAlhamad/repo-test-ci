# Generate Command

This directory contains commands for generating code templates, such as job templates for the jobs API.

## Usage

Run the following command to start the interactive generator:

```bash
npx shield-cli generate
```

You will be prompted to select which entity you want to create (e.g., CronJob, API, Component). Based on your selection, you will be guided through the required steps.
